package com.example.camerasdoors.app.ui.theme

import androidx.compose.ui.text.font.Font
import androidx.compose.ui.text.font.FontFamily
import com.example.camerasdoors.R

val CirceRegular = FontFamily(
    fonts = listOf(
        element = Font(
            resId = R.font.circe_regular
        )
    )
)